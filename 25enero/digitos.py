import os
os.system('cls')

#Escriba un programa que determine la cantidad de dígitos en un número entero ingresado por el usuario:

#Esta parte es para mostrar uno por uno la cantidad de digitos
# digitos = (input("Ingrese un numero: "))
# for i in range (len(digitos)):
#     print(digitos[i])

import os
os.system('cls')

#Escriba un programa que determine la cantidad de dígitos en un número entero ingresado por el usuario:

digitos = (input("Ingrese un numero: "))

#len es un contador de caracteres
print(f"{digitos} tiene {len(digitos)} digitos")

